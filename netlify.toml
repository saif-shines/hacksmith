
[build]
  base = "apps/website"
  command = "curl -fsSL https://d2lang.com/install.sh | sh -s -- && pnpm run build"
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
