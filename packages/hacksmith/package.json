{
  "name": "hacksmith",
  "version": "0.0.3-0",
  "private": false,
  "type": "module",
  "bin": {
    "hacksmith": "bin/hacksmith"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "dev": "pnpm build && node --enable-source-maps dist/run.js",
    "build": "tsc -p tsconfig.json",
    "test": "pnpm build && bun test",
    "prepack": "pnpm build",
    "link:global": "npm link",
    "unlink:global": "npm unlink -g hacksmith",
    "publish:np": "pnpm dlx np"
  },
  "engines": {
    "node": ">=18",
    "bun": ">=1.1.0"
  },
  "dependencies": {
    "@clack/core": "^0.5.0",
    "@clack/prompts": "^0.11.0",
    "@oclif/core": "^4.5.3",
    "oclif": "^4.22.22"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "oclif": {
    "bin": "hacksmith",
    "commands": "./dist/commands"
  }
}
